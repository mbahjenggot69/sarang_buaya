#include <graphics.h>

int main(){
	int = DETECT;
	int gm
	initgraph(&gd, &gm, "C:\\TC\\BGI");

	int midX = getmaxx() / 2;
	int midY = getmaxy() / 2;

	int width = textwidth("VCR Games");
	int height = text height("VCR Games");

	outtextxy(midX, midY, "VCR Games");

	getch();
	closegraph();
}
