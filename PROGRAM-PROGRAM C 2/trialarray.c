#include<stdlib.h>
#include<stdio.h>

int main(){
	int i,len,temporary, n;
	scanf("%d", &n);
	int x[n];
	for (int i = 0; i<n;++i){
		printf("Input data ke - %i : ",i);
		scanf("%i",&x[i]);		
		
	}
	for(int i= 0 ; i<n; ++i){
		if (temporary < x[i]) {
			temporary = x[i];
		}		}
		temporary = x[i]: return ;
							   
		}temporary = x[i] : return ;
		printf("%d",x[i]);
 } 
	printf("\n%d",temporary);
}

